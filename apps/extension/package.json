{
  "name": "cluttarex-extension",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && vite build && mv dist/src/popup/popup.html dist/popup.html && sed -i '' 's|../../popup.js|./popup.js|g' dist/popup.html && rm -rf dist/src && cp manifest.json dist/manifest.json",
    "dev": "vite"
  },
  "dependencies": {
    "@cluttarex/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "@types/chrome": "^0.0.260"
  }
}
