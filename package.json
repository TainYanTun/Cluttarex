{
  "name": "cluttarex-monorepo",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "postbuild": "cd apps/extension/dist && zip -r ../../../apps/web/public/cluttarex-extension.zip .",
    "lint": "bun run --filter '*' lint"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "prettier": "^3.0.0"
  }
}
